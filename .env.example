# =============================================================================
# KOLLECT-IT MARKETPLACE - ENVIRONMENT VARIABLES
# =============================================================================
#
# ⚠️  SECURITY WARNING:
# - Fill these values LOCALLY and NEVER commit real secrets to Git
# - For Netlify: Set the same variable NAMES in Site → Settings → Environment variables
# - This file (.env.example) is safe to commit - it contains NO real values
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Supabase/Neon PostgreSQL)
# -----------------------------------------------------------------------------
# Get from: Supabase Project Settings → Database → Connection String
#
# POOLED connection (port 6543) - Used by application for all queries
# MUST include ?pgbouncer=true for connection pooling
DATABASE_URL="postgresql://postgres:<PASSWORD>@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# DIRECT connection (port 5432) - Used ONLY for migrations (local execution)
# Do NOT use pooled connection for migrations
DIRECT_URL="postgresql://postgres:<PASSWORD>@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# -----------------------------------------------------------------------------
# AUTHENTICATION (NextAuth.js)
# -----------------------------------------------------------------------------
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# Example output: wK9x5vN2mP8qR3tY6u7Z0A1b4C5d8E9f (use a fresh one!)
NEXTAUTH_SECRET="<GENERATE_WITH: openssl rand -base64 32>"

# Set NEXTAUTH_URL to your site URL AFTER first deploy
# Local dev: http://localhost:3000
# Netlify: https://your-site.netlify.app (update after first deploy)
NEXTAUTH_URL="<SET_AFTER_FIRST_DEPLOY>"

# -----------------------------------------------------------------------------
# PAYMENTS (Stripe)
# -----------------------------------------------------------------------------
# Get from: https://dashboard.stripe.com/test/apikeys
# ⚠️  START WITH TEST KEYS - Switch to live keys only after full verification
#
# Public key (safe to expose in client-side code)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Secret key (NEVER expose - server-side only)
STRIPE_SECRET_KEY="sk_test_..."

# -----------------------------------------------------------------------------
# EMAIL (Resend)
# -----------------------------------------------------------------------------
# Get from: https://resend.com/api-keys
RESEND_API_KEY="re_..."

# Email "From" address - Format: "Name <email@domain.com>"
# For testing: Use "Kollect-It <onboarding@resend.dev>"
# For production: Verify your domain in Resend first
EMAIL_FROM="Kollect-It <noreply@your-verified-domain.com>"

# Admin email - Where order notifications are sent
ADMIN_EMAIL="admin@your-domain.com"

# -----------------------------------------------------------------------------
# IMAGES (ImageKit CDN)
# -----------------------------------------------------------------------------
# Get from: https://imagekit.io/dashboard → Developer options
#
# URL Endpoint (must end with /)
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT="https://ik.imagekit.io/<YOUR_IMAGEKIT_ID>/"

# Public key (safe to expose)
NEXT_PUBLIC_IMAGEKIT_PUBLIC_KEY="public_..."

# Private key (NEVER expose - server-side only)
IMAGEKIT_PRIVATE_KEY="private_..."

# -----------------------------------------------------------------------------
# NODE ENVIRONMENT
# -----------------------------------------------------------------------------
# Set to "production" for deployment, "development" for local dev
NODE_ENV="production"

# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS
# -----------------------------------------------------------------------------
# 1. Copy this file to .env (local development):
#    cp .env.example .env
#
# 2. Fill in REAL values in .env (NEVER commit this file)
#
# 3. For Netlify deployment:
#    - Go to Site Settings → Environment variables
#    - Add each variable NAME from above
#    - Paste REAL values in Netlify UI
#
# 4. Verify setup:
#    - Local: bun run dev
#    - Netlify: Visit https://your-site.netlify.app/api/health
#
# =============================================================================
