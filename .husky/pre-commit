#!/usr/bin/env sh

# Auto-format, fix markdown, type-check, and lint
bun run format
# Run markdownlint but don't block commits for now (too many legacy docs)
bun run lint:md:fix || true
bun run type-check
bun run lint
